#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../nunjucks/bin/precompile" "$@"
  ret=$?
else 
  node  "$basedir/../nunjucks/bin/precompile" "$@"
  ret=$?
fi
exit $ret
