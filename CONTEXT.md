# Awesome Resources

个人资源收录仓库。本 glossary 定义「收录标准」中的核心术语——它们会写在 README 顶部的收录要求 section 中，是 PR 审核的判断依据；另定义条目与分类的结构术语，以及链接检查的判定术语。

## Language

### 收录标准

**免费 (Free)**:
资源有可用的免费层即可收录（freemium 合格）；不要求完全免费，也不要求开源。
_Avoid_: 完全免费、开源（开源是另一回事，不是收录条件）

**非商业化 (Non-commercial)**:
资源不以「诱导付费转化」为主要目的。纯导流站、广告站、联盟营销站排除。商业公司出品的免费可用产品（如 MDN、Google Developers）不算商业化。
_Avoid_: 无商业背景、无广告（都不是本术语的含义）

**简短 (One-liner)**:
每条资源的描述压缩为一句话。
_Avoid_: 无描述、长简介

**渐进清理 (Gradual cleanup)**:
收录标准对新增条目生效；存量条目暂保留，遇到明显违规或失效的条目顺手移除，不做一次性全量清理。
_Avoid_: 追溯清理、全量审查

**补齐 (Backfill)**:
为存量缺描述条目补写一句话描述的行为。描述取自站点自身的自述（HTML meta description 优先），保留原语言；只补不修——不改动条目的链接与名称。
_Avoid_: 修复（损坏链接的修整不是补齐）、改写（重写已有描述不是补齐）

**收录 (Inclusion)**:
向 README.md 新增条目的行为（websites.md 已归档，不再收录）；收录前必须查重——该 URL 未在 README 中出现过。
_Avoid_: 添加（未体现查重前提）

### 条目与分类

**条目 (Entry)**:
列表文件中的单条资源记录，格式 `- [名称](URL) - 一句话描述`，描述遵循「简短」。
_Avoid_: 链接（链接仅指条目中的 URL 部分）

**分类 (Section)**:
README.md 中由顶部 TOC 索引的内容分区；新增分类须同步更新 TOC，且至少容纳 3 个条目。
_Avoid_: 目录（TOC 是指向分类的索引，不是分类本身）

### 链接检查

**失效链接 (Broken link)**:
链接检查中服务器明确返回 HTTP 4xx/5xx（403、429 除外）的判定，是资源不存在或服务端故障的强信号。是「渐进清理」的触发信号；检查任务只报告，不修改 README，移除或修复由人工顺手处理。
_Avoid_: 坏链（口语）、无法访问（易与「无法验证」混淆）、连接拒绝（连接级失败取决于探测出口，不是失效）

**无法验证 (Unverifiable)**:
链接检查未能完成 HTTP 校验的判定——反爬拦截（403/429）、超时、TLS 异常、HTTP/2 协议错误、无效响应、连接被关闭/拒绝/失败。这些结果依赖探测出口（数据中心 IP 信誉、反爬、墙），站点可能存活，须人工复核，不计入「失效」。
_Avoid_: 失效（把反爬站或被出口拦截的站误当死链）、临时故障（暗示会自愈，无依据）

**格式异常 (Malformed URL)**:
条目 URL 缺少 `http(s)://` 前缀、因而未参与链接检查的记录。
_Avoid_: 失效链接（未检查 ≠ 不可达）
